services:
  - type: web
    name: HypeSniff
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars: []
    healthCheckPath: /
